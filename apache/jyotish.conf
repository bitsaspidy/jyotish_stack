# /etc/apache2/sites-available/jyotishstack.conf
# Enable:
#   sudo cp apache/jyotish.conf /etc/apache2/sites-available/jyotishstack.conf
#   sudo a2ensite jyotishstack
# Required modules:
#   sudo a2enmod proxy proxy_http proxy_wstunnel rewrite ssl headers
#
# Run Certbot after DNS points to this VPS:
#   sudo certbot --apache -d jyotishstack.com -d www.jyotishstack.com --redirect

<VirtualHost *:80>
    ServerName jyotishstack.com
    ServerAlias www.jyotishstack.com

    # Canonical host: www → non-www, 301.
    # www.jyotishstack.com currently serves 200 (it is a ServerAlias), so the site
    # answers on two hostnames — duplicate content that splits crawl signals. The
    # canonical <link> already points at non-www, which mitigates it, but a 301 is
    # the actual fix. mod_rewrite must be enabled: `sudo a2enmod rewrite`.
    # ⚠️ Certbot copies THIS vhost to the :443 SSL vhost it generates; this rule
    # must also be present there, or www is only redirected on plain http. After
    # editing, re-run `sudo certbot --apache` or add the same 3 lines to the
    # :443 vhost by hand, then `sudo systemctl reload apache2`.
    RewriteEngine On
    RewriteCond %{HTTP_HOST} ^www\.jyotishstack\.com$ [NC]
    RewriteRule ^ https://jyotishstack.com%{REQUEST_URI} [R=301,L]

    ProxyPreserveHost On
    ProxyTimeout 120

    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set Referrer-Policy "strict-origin"

    <Location /api/>
        ProxyPass        http://127.0.0.1:5000/api/
        ProxyPassReverse http://127.0.0.1:5000/api/
    </Location>

    <Location /_next/static/>
        ProxyPass        http://127.0.0.1:3000/_next/static/
        ProxyPassReverse http://127.0.0.1:3000/_next/static/
        Header set Cache-Control "public, max-age=31536000, immutable"
    </Location>

    ProxyPass        / http://127.0.0.1:3000/
    ProxyPassReverse / http://127.0.0.1:3000/

    ErrorLog  ${APACHE_LOG_DIR}/jyotishstack_error.log
    CustomLog ${APACHE_LOG_DIR}/jyotishstack_access.log combined
</VirtualHost>
